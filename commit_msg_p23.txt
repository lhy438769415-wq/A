[chore] #23 P3 文档/低优收尾: 门禁加固 + calculator告警 + stdout互踩 + 文档矛盾 + STATUS刷新

① 门禁加固 (.agent/quality_gate.py)
   - import* 正则由 `from\s+\w[\w.]*` 改为 `from\s+[.\w]+`, 现可捕获相对导入 `from ... import *`
     (此前审计漏检 gui_dashboard.py 的根因即相对导入不匹配)
   - 新增 scan_get_logger_order: 模块级 get_logger(__name__) 调用早于其 import 即报错,
     闭环此前回归护栏缺口; 排除 core/log_config.py 自身避免自指误报
   - 自测: 相对/绝对 import* 均命中, 显式导入不误报; get_logger 顺序违例可捕获, log_config 豁免

② gui_dashboard.py:20 import* —— 经 grep 确认全保留代码已无任何 import* (审计行号陈旧),
   该文件当前为显式括号导入, 项已自然满足

③ calculator.py:240 RuntimeWarning —— linreg `slope = cov_xy / var_x` 在前 window-1 行
   rolling 未填满时为 NaN/NaN, 触发 invalid value RuntimeWarning; 用 np.errstate 抑制,
   结果仍为 NaN, 零行为变化 (pytest 以 -W error::RuntimeWarning 验证通过)

④ stdout/stderr 互踩 (tools/scanner_weekly_gap.py:20 + scanner_weekly_3k.py:16)
   - 模块顶层 `sys.stdout = io.TextIOWrapper(...)` 改为惰性: 仅 `if __name__=='__main__'` 时执行,
     避免被其他模块导入时在进程内互相覆盖全局流 (对齐 hunter.py 的 reconfigure 安全做法)
   - 同款问题在 tests/test_bs_net.py / test_split_msg.py 顶层亦存在, 一并改为惰性,
     避免破坏 pytest 的 stdout capture

⑤ 文档矛盾修正
   - .agent/context/STATUS.md: 最后更新日期、Harness Phase2 标注为"部分完成(质量门禁已实现)"、
     架构债务表 P4-P11 据门禁实测刷新为已修复、回归基线 25/25 -> 156 测试函数数守卫
   - docs/CHANGELOG_agent_handoff.md: V9.8 -> V9.20
   - code_review_report.md: 迭代至 V9.15 -> V9.20
   - 新增 docs/LOGIC_AUDIT_RULES.md: 补全被引用却缺失的 SSOT 逻辑审计规则
     (PA volume 禁入信号/评级、评级因子白黑名单、UX 评审硬约束、工程护栏), 终结文档矛盾

⑥ Phase2/Phase3 启用: 随 STATUS.md Harness 段刷新落地 (质量门禁 = Phase2 基础设施已实现;
   CI/自动门禁 hook/监控仍属待建, 未夸大 Completion)

验证: py_compile 全绿; 质量门禁 0 红线 / 156 测试守卫通过;
pytest tests/test_calculator.py+test_split_msg.py 17 passed (含 -W error::RuntimeWarning);
pytest 三件套 111 passed
