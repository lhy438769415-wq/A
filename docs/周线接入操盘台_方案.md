# 周线接入操盘台 — 实施方案 v2（2026-08-30 复核修订）

> v1 定稿于 2026-08-30 上午。本版是在通读完全部项目规矩后**重新核查数据与代码**的产物：
> **三项已拍板决策全部成立**，但核查中挖出 **5 个 v1 未覆盖的事实**，其中 2 个会直接影响你看到的结果。
> 本版新增"数据治理"一步，并新增**三个需要你拍板的新问题**（时间窗口 / 推送去重 / 清单切法）。
> ⚠️ 其中"清单切法"（问题 C）是初稿被实测推翻后补的：周线信号不少，是**按天切片这个看法的错**。

---

## 〇、与 v1 的差异（只看这段也行）

| # | v1 的说法 | v2 复核后的真相 | 影响 |
|---|---|---|---|
| 1 | 周线耗时"未知，先测 100 只" | **已实测**：同步约 30 秒、缺口扫描约 5 分钟、3K 扫描约 3 分钟（全市场 3312 只） | 成本可接受，不再是风险 |
| 2 | 库里有 3148 条周线信号（当成可用资产） | **2622 条（83%）是 2026-05 一次性回测回填**，signal_date 横跨 2011~2026，2611 条已判 WIN | **直接切周线会看到 2011 年的旧账**，必须先处理 |
| 3 | 周线扫描"检查最近 4 周" | **实际回看 60 周**（`scan_engine.py:161` 的 `signal_lookback=60`，4 周参数没往下传） | 每次扫出的是**近 14 个月内所有仍存活的信号**，不只本周 |
| 4 | 默认日线、周线手动切 | 成立，但补一条：**周线只有周五收盘后跑才有新信息**，周一~周四跑是白跑 | 状态栏应显示"周线数据截至哪一周" |
| 5 | 数据只提到"674 条脏日期" | 另有一批信号把「结构性缺口 GAP H1」记成了早期内部写法（非现役 key） | 界面按现役 key 统计会**整批漏掉**（周线实盘信号里 469 条） |
| 6 | 每周产出"1~7 条" | **口径错了**。29 条是扫出量，1~7 条是"新增归档量" | 周线页面不会空，详见发现 3 |

---

## 一、你已拍板的三项决策（复核：全部成立，不改）

| # | 决策项 | 结论 | 复核 |
|---|---|---|---|
| 1 | 日线/周线怎么分开 | 顶部切换，完全隔离 | ✅ 成立 |
| 2 | 周线跑哪天 / 是否自动化 | 纯手动，不做定时 | ✅ 成立 |
| 3 | 扫描范围 | 全市场 3312 只 | ✅ 成立（实测耗时支持） |

### 两条红线（不变）

- **红线 A：不含任何"自动"行为。** 不按星期自动切换、不开机自动跑、不定时提醒、不弹窗催促。
- **红线 B：状态栏只报数，不催你做事。** 只显示"周线数据截至 08-28"，不出现"本周还没跑，建议跑"。

---

## 二、实测数据（本机实跑，非估计）

| 动作 | 实测样本 | 实测耗时 | 推算全市场 3312 只 |
|---|---|---|---|
| 周线行情同步 | 存量扫描 3312 只 | 1.9 秒 | **约 30 秒**（含本地聚合） |
| 周线缺口扫描（3 策略） | 100 只 7.2 秒 / 300 只 26.9 秒 | — | **约 5 分钟**（4 线程并行） |
| 周线 3K 扫描 | 30 只 1.7 秒 / 60 只 3.2 秒 | — | **约 3 分钟**（串行） |

> 合计：一次完整周线作业（同步 + 全策略扫描）**约 6~8 分钟**。
> 大白话：跟你现在每天跑日线是同一个量级，不用特意留出大块时间。

**补充**：前 300 只实测命中 **0 条**（样本靠前、不代表全市场）。全市场实测产出见发现 3。

---

## 三、五个新发现（v1 未覆盖，全部实测/读码）

### 发现 1：库里 86% 的周线信号是回测回填，不是实盘信号 🔴

- 3148 条周线信号中，**2622 条入库于 2026-05**（一次性），**526 条**来自每周实盘扫描。
- 这 2622 条的 signal_date 横跨 **2011-04-01 ~ 2026-05-29**，其中 **2611 条状态是 WIN**。
- 判定依据：signal_date 跨度 15 年、状态几乎全 WIN、scan_date 被写成 2014/2015 年的历史日期（模拟历史扫描）。

**影响**：方案 v1 第 3 步"所有查询加 `AND timeframe='weekly'`"做完后，
你切到周线，日期下拉框里会出现 2011 年起的几百个日期，清单里躺着一堆早已平仓的历史信号。
**必须先做数据治理**，否则这个页面没法用。

### 发现 2：周线扫描是"回看 60 周"，不是"只看本周" 🔴

- `scan_engine.py:161` `scan_single_code_weekly(code, signal_lookback=60, ...)`，
  函数注释明确写："缺口家族需回看较宽窗口(默认60周)以捕捉仍存活的 pending/active 信号"。
- `scan_weekly_gap_signals` 的 `recent_weeks=4` 参数**根本没传给单票函数**（`:363` 只传了 code 和 strategies），
  所以界面上写"检查最近 4 周"是**假的**，实际回看 60 周。
- 证据：2026-08-01 那次扫描，归档了 signal_date 为 `2025-11-28`、`2025-09-19`、`2025-08-15` 的信号（几个月前的）。

**影响**：
- 这不是 bug，是缺口策略的设计意图（"信号还活着"就有意义，跟日线"只推当天触发"的口径不同）。
- 但**归档是幂等的**（`signal_id = code_strategy_timeframe_signal_date` + `INSERT OR IGNORE`，`archive.py:32/48`），
  所以**不会重复入库**；而 **Discord 推送是全量推**（`scan_engine.py:595` 起 `for s in sig_gap`），
  **每周会把近 60 周的老信号重新推一遍**。

> 这是"每周跑一次周线"最实际的副作用：**Discord 会收到一堆几个月前的旧信号**。
> 是否要处理，取决于你——见第六节待拍板问题 B。

### 发现 3：每次全市场扫描扫出约 29 条，不是"一周两三只" ⚠️ 口径已修正

**v2 初稿在这里写错了，现按实测更正。** 初稿拿"归档进库的条数"当成了"扫描产出"，
但归档有两个特性会严重压低这个数字：
① 只归档**已确认**信号，待确认（未破位）的 16 条不进库；
② 归档是幂等的（同一条信号重复扫只记一次），上周归过的这周不重复入库。

**实测（2026-08-30，全市场 3312 只，本机实跑 6.7 分钟）：**

| 口径 | 数量 | 说明 |
|---|---|---|
| **扫描扫出总数** | **29 条** | 这是界面/推送里能看到的数量级 |
| — 结构性缺口 GAP H1 | 24 条 | |
| — GAP H2 | 3 条 | |
| — GAP PINBAR | 1 条 | |
| — 3K | 1 条 | 3K 路径不归档进库 |
| 其中 待确认（未破位） | 16 条 | 👀 只进监控名单，不推送归档与否见下 |
| 其中 已确认 | 12 条 | ✨ 会推送 + 会归档 |

> 大白话：每周跑一次周线，你会看到**三十来只**票，其中约一半已经破位确认、另一半还在等触发。
> 周线页面**不会空**。

**由此引出两个必须说清的差异**：

**① 界面看不到那 16 条"待确认"**。界面读的是库，库里只有已确认信号（这是你 7-30 定的
"只归档通过信号"），所以待确认的票只在推送和监控名单文件里，操盘台里没有。
要让它进界面是另一个改动，需你另外拍板（本次不改）。

**② 更关键：29 条是散落在 60 周里的，不是集中在某一周。** 🔴

单次扫描扫出的 29 条，分布在 60 周回看窗口的**不同周**上。库里累计 526 条真实信号，
分布在 **59 个信号日**上，平均每个信号日只有几条：

| 信号日 | 条数 |
|---|---|
| 2026-08-21 | 1 |
| 2026-08-14 | 1 |
| 2026-08-07 | 2 |
| 2026-07-24 | 1 |
| 2026-07-17 | 1 |
| 2026-07-10 | 7 |
| 2026-07-03 | 7 |

> 也就是说：界面沿用日线那套"选某一天 → 看那天的信号"，切到周线后**选中最新周只能看到 1 条**。
> 这就是"看着少"的真正原因 —— 不是信号少，是**按单日切片的展示方式不适合周线**。
> → 需要给周线单独设计列表口径，见第六节新增的问题 C。

### 发现 4：数据质量有两处硬伤

| 问题 | 数量 | 现状 | 是否阻塞 |
|---|---|---|---|
| signal_date 是垃圾值（`99`/`97`/`90`/`762` 等） | 674 条 | 现有 `_DATE_LIKE = "____-__-__"` 已能过滤 | ❌ 已有解 |
| 一批信号把「结构性缺口 GAP H1」记成了**早期内部写法**，未被现行 key 收录 | 周线实盘里 **469 条** | 界面按现役 key 统计 → **整批漏掉** | ✅ 需在查询里归一 |

> 这不是"两个不同策略"，是**同一个策略（花名 GAP H1）的两种内部记法**。
> 处理方式：查询时归一到现役 key，不新增、不改动任何策略名。
> 日线同样存在历史遗留记法（`MTR_MASTER` 与早期写法并存、`UNKNOWN`、`TEST_STRAT`），
> 但按"不发散"原则，**本次只处理周线这批**，日线的另开一次。

### 发现 5：周线同步的成本结构

- `update_weekly_data_batch(max_workers)` **只接受一个参数**，不支持 `limit` / `all_codes`（`:1140`），
  → 只能全市场跑，无法"先试 100 只"。
- 当前状态：3312 只中 **3190 只已是最新**（2026-08-28），**122 只待更新**——
  这 122 只是长期停牌/退市股（000004、000005、000023…），每周都会被尝试更新。
- 同步主流程是**拿本地日线数据在内存里合并成周线**（`_fast_local_weekly_aggregation`，`:1060`），不走网络。

> 影响：同步本身很便宜；那 122 只停牌股每周空转一次，是小浪费，不阻塞。

---

## 四、需求本身的重新评估

### 结论：这个需求值得做，而且价值比 v1 估计的更高

项目自己的评级研究结论（15 年样本）：
**周线缺口家族期望收益为正**（H2 +0.381R / 结构性 +0.295R / Pinbar +0.482R），
**而日线普遍偏弱、部分为负**。

> 大白话：你这套系统里，周线才是真正有优势的那一块，日线是弱项。
> 但现在周线只能靠命令行跑、结果只在 Discord 看，桌面操盘台上**完全没有周线的位置**。
> 所以"补周线"不是补一个锦上添花的功能，而是**把你最有优势的武器搬上桌面**。

### 但有三个认知需要修正

**① "每周跑一次"是对的，但理由要换。**
v1 隐含假设是"周线很贵，所以少跑"。实测下来它并不贵（6~8 分钟）。
真正的理由是：**周线要等周五收盘才有新的一根 K 线**，周一~周四跑，数据是旧的，结果跟上周完全一样，等于白跑。
→ 所以正确的时机是**周五收盘后或周末**，一周一次就够，不是因为贵，是因为**没新数据**。

**② 默认停在"日线"仍然合理，但你可能低估了周线。**
默认日线是为了不打乱你现在的每天习惯（打开就能用）。保持不变。
但由于周线期望收益为正、日线弱，**周线页面可能才是你真正该多看的地方**。
→ v2 建议：状态栏在周线模式下，把"周线数据截至哪一周"显示出来，让你一眼知道该不该跑。

**③ 零自动化带来一个你需要知道的成本。**
程序不会提醒你（你定的红线 A）。所以"周六记得切到周线跑一次"完全靠你自己记。
→ 这是你自己选的代价。如果你哪天想加提醒，随时可以改，但现在按红线不做。

---

## 五、修订后的实施步骤（在 v1 的 4 步前插入 1 步）

| 顺序 | 动作 | 变化 |
|---|---|---|
| **0（新）** | **数据治理：给 2622 条回测回填做标记，并统一策略名** | v1 没有。不做这步，周线页面就是一堆 2011 年的旧账 |
| 1 | 底层三处改造：周线同步/扫描加进度+可终止；3K 声明改 `['daily','weekly']` | 同 v1 |
| 2 | 界面加「日线 \| 周线」切换（默认日线，手动切） | 同 v1 |
| 3 | 所有查询加 `AND timeframe=?` 隔离 | 同 v1 |
| 4 | 状态栏补两条纯信息：周线数据截至 X / 上次周线扫描 Y | 同 v1 |
| 5 | 你手动切到周线实跑一次全市场，我陪同看进度条 | 同 v1（实测约 6~8 分钟） |

### 第 0 步怎么做（两种方案，需你选）

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **甲：给回填数据打标记** | 给 2622 条加 `extra_json` 标记 `backfill:true`，界面默认过滤掉，另设"显示历史"开关 | 数据不丢，回测还能用 | 需要改界面查询逻辑 |
| **乙：直接归档隔离** | 把这 2622 条移到 `signal_archive_backfill` 表（新建），主表只留实盘 | 主表干净，查询零改动 | 动了历史数据；回测脚本要改读表 |

> 我的建议：**甲**。不动历史数据（符合"数据只增不改"的保守原则），只是默认不显示。

---

## 六、三个需要你拍板的新问题

### 问题 A：周线清单显示哪些信号？（必须选一个）

| 选项 | 含义 | 你会看到 |
|---|---|---|
| **A1（推荐）** | 排除回测回填 + 只显示最近 12 周 | 干净，能看到近三个月的信号 |
| A2 | 排除回测回填 + 显示最近 60 周（对齐扫描窗口） | 与引擎口径一致，但会看到一年前的旧信号 |
| A3 | 排除回测回填 + 全部显示 | 从 2026 年起的实盘信号全在 |

> 三者都排除 2622 条回填数据（第 0 步的活）。差别只在时间窗口。

### 问题 B：Discord 重复推旧信号，要不要管？

现状：每周跑周线，Discord 会把近 60 周内所有信号（含几个月前的）**重新推一遍**。

| 选项 | 做法 |
|---|---|
| **B1（推荐）** | 加一道"只推本周新出现的"过滤，跟日线口径对齐 |
| B2 | 保持现状（每周全量推），理由是"老信号还活着就该看" |

> 注意：B 属于推送侧改动，与操盘台界面无关。按"不发散"原则，**你可以选 B2 先不动**。

### 问题 C：周线清单怎么切？（新增，这是"看着少"的病根）

界面现在是「选某一天 → 看那天的信号」。日线没问题（每天都有新信号），
但周线的信号是**散落在 60 周里的**：最近一周只有 1 条，往前数几周才凑得齐。
沿用日线那套，你切到周线、默认停在最新一周，屏幕上就孤零零一条 —— **不是信号少，是切法不对**。

| 选项 | 做法 | 你会看到 | 改动量 |
|---|---|---|---|
| **C1（推荐）** | 周线模式下，日期下拉**前面加几档窗口**：近 4 周 / 近 12 周 / 近 60 周 / 指定某一周。默认「近 12 周」。清单按周分组，每组小标题写"08-21 那周 · 共 N 条" | 一屏看全最近三个月的信号，仍知道每条是哪周触发的 | 中（只改周线模式，日线零影响） |
| C2 | 不分日期，按**信号当前状态**分栏：待确认 / 已确认 / 已结束 | 直接回答"哪些还活着" | 大（且**待确认信号目前没进库**，需先改归档规则，本次不做） |
| C3 | 什么都不改，只在上头加一行字"近 12 周共 M 条" | 仍然是选一周看一条 | 极小 |

> 大白话：C1 相当于把"翻到某一天、看一条"，换成"把最近三个月摊开一次看完"。
> C2 其实更贴合周线"信号会活好几周"的特点，但它要求先把待确认信号也记进库里，是另一件事。
> 所以我建议 **C1**：改动可控，日线那套一点不动。

**C1 与问题 A 的关系**：如果你选 C1，问题 A 的"12 周 / 60 周 / 全部"就直接变成 C1 下拉里的档位，
两个问题合并成同一个开关（默认近 12 周），**你不用再单独回答 A**。

---

## 七、明确不做的事（不变）

- 不给 MTR / AIL 加周线能力（策略层面没做过周线验证）
- 不改任何策略的判定逻辑
- **不做任何自动化**：不定时、不按星期自动切、不开机自动跑、不弹窗、不主动建议
- 不顺手清理日线的历史脏数据（`UNKNOWN` / `TEST_STRAT` / MTR 双名），另开一次
- 不动周线扫描的 60 周回看窗口（那是策略设计，不是 bug）

---

## 八、验收标准（在 v1 基础上加 2 条）

| # | 标准 |
|---|---|
| 1 | 打开后默认停在"日线"，与今天行为完全一致 |
| 2 | 切到"周线"，「下载行情」有进度百分比，点「终止」能真停 |
| 3 | 切到"周线"，「策略扫描」有进度百分比，中途可终止（全程约 6~8 分钟） |
| 4 | 周线信号只出现在周线模式，切回日线看不到（反之亦然） |
| 5 | 日线模式所有行为与今天完全一致（回归无影响） |
| 6 | 状态栏显示"周线数据截至 X / 上次周线扫描 Y"，**没有任何弹窗或自动切换** |
| 7 | **（新）周线模式下不会看到 2011~2025 的回测回填信号**（第 0 步生效） |
| 8 | **（新）周线模式下 GAP H1 的 469 条全部计入，不漏** |
| 9 | 质量门禁通过；用系统 Python 3.14 跑构造界面冒烟，不报 TclError |

---

## 九、一句话总结

周线该做，而且是你这套系统里更有优势的那块；全市场跑一次只要 6~8 分钟，不贵，每周扫出三十来条，页面不会空。
但动手前必须先做一件事：**把库里 2622 条 2011 年起的历史回测回填数据跟实盘信号分开**，否则切到周线就是翻旧账。
另外请你拍板三件事：**A 显示多久内的信号**、**B Discord 要不要停止重复推旧信号**、
**C 周线清单按"周窗口"摊开看还是按"单周"切**（C 是"看着少"的真正解法，建议 C1）。
