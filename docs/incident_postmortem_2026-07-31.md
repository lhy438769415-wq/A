# 2026-07-31 晚间事故复盘报告

> 写给:交易员(非程序员)。全文大白话,所有技术词第一次出现都带解释。

## 一句话结论

**你的怀疑是对的——在今晚修复之前,系统健康度确实严重不足**:代码没做过任何版本备份(一个文件丢了就几乎找不回)、行情数据库没有任何备份副本、还有个没查清的"外部程序会偷偷删文件"的隐患。这三条叠在一起,就是今晚一个核心文件被清空后差点不可挽回的根本原因。**今晚的修复把最大的三个洞都补上了**,但还剩两个小坑要处理。

---

## 一、今晚发生了什么(按时间线)

### 事件 1:核心文件 data_provider.py 被发现清空(约 20:49)

- **这个文件是什么**:它是"数据管家",负责从本地数据库读行情、批量下载更新数据。是整个扫描器最核心的几个文件之一。
- **现象**:你想跑扫描器时,发现这个文件内容空了(被截断)。
- **怎么查的**:
  - 翻遍了编辑器临时文件、备份文件——没有。
  - 查 git(就是帮你存版本历史的工具)——这个仓库**从来没有提交过任何版本**("No commits yet"),所以 git 里根本没有任何历史副本可恢复。索引里指向的那个副本也已经损坏丢失。
  - 看了同目录其他文件的修改时间——**只有 data_provider.py 一个文件是新的,其他全是旧时间**。
- **根因结论**:**某个外部程序(最可能是编辑器或网盘同步工具)把这一个文件截断了**。爆炸范围就这一个文件,不是磁盘级损坏。**但确切是哪个程序干的,没有找到日志证据,无法 100% 锁定。**
- **是不是我(助手)弄的**:不是。本会话开始前我只动过 hunter.py(另一个文件),没碰过 data_provider.py。

### 事件 2:第一次恢复——只救回 7 成(约 21:xx)

- 从 git 的一个压缩包残骸里,救回了 **7 月 25 日的旧版本**。
- **问题**:这个版本少了 7 月 26 日当天做的 10 处功能增强(比如周线数据读取、连接断开的精准处理)。能用,但不完整。

### 事件 3:第二次恢复——逐字节精确还原(约 22:00)

- **关键发现**:清空之前,Python 留下了一个"编译缓存文件"(`.pyc`,就是源码被翻译成机器能跑的中间形式的快照),时间戳是 7 月 26 日 14:04——**正好是清空前的最后状态**。
- **做法**:把这个缓存文件反汇编(就是把机器码倒推回可读逻辑),逐个函数对照,把 7 月 26 日那 10 处增强精确补回去。
- **验证**:用 Python 3.14 把恢复版和那个缓存快照做**逐字节比对**——**8 个函数全部完全一致**。即恢复版 = 清空前的精确状态 + 之后的修复。
- **结果**:质量门禁通过、相关测试通过。文件救回到 50559 字节 / 1286 行,完整可用。

### 事件 4:你怀疑"清理弹窗"是元凶——我做了取证(约 22:1x)

- **你的怀疑**:做那两个任务(补 0 命中策略告知 + 推送结构对比 demo)时,弹出过一个"清理 XX"的选项,你点了允许,怀疑就是它清空了文件。
- **我怎么查的**:翻了 WorkBuddy 自己的**命令审计日志**(`~/.workbuddy/audit-log/2026-07-31.jsonl`,记录了当天每一次命令执行 + 你的批准),以及本地数据库。
- **结论**:**那次"清理"弹窗没有清空 data_provider.py**。全天带"清理"字样的命令只有 22:12–22:15 那几次,删的是我自己恢复时用的临时脚手架文件(`_recover_base.py` / `_recover_script.py`),跟你的源码一点关系都没有。你做那两个任务的时段(15:33–20:49)里,日志里**零条**跟本项目相关的命令。
- **诚实的保留**:对话正文没存在本地数据库里,所以我**无法 100% 排除**"我用文件编辑工具手滑清空了它"这种极端假设。但那两个任务是改别的文件,没有任何理由去碰核心源码;审计日志也找不到对应的删除命令。综合判定:**清理弹窗是误判,清空与那次"允许清理"无关。**

### 事件 5:对比原始备份,证明非迭代代码没被动(约 22:2x)

- 拿恢复版跟 7 月 25 日的 git 基底、以及 antigravity 原始备份做三重对比。
- **结论**:相对于"截断前基底",**非迭代部分一字未动**——所有差异都落在已知的 9 处迭代改动内。相对 antigravity 老备份的差异是历史演进(7/25、7/26 你本来的工作),不是我引入的。

### 事件 6:加防截断守卫 + 写预防建议(约 22:3x)

- 新增 `hooks/guard_source_shrink.py`:每次提交前自动检查,任何一个代码文件如果是空的、或者行数比上次少了超过一半,**直接拦住不让提交**。这样即使文件再被截断,也进不了版本库、不会静默丢失。
- 写了 `docs/accident_prevention_constraints.md`(预防建议)。

### 事件 7:第一次正式提交——堵住"不可恢复"的根(约 22:4x)

- **根因**:今晚之所以差点找不回文件,根本原因就是**这个仓库从来没提交过任何版本**——git 里的对象只被索引引用,没人提交就会被认为是垃圾被清理掉(丢失的 `b89a3453` 就是一例)。
- **做法**:清空陈旧损坏的索引,从磁盘重新生成有效对象,完成**第一次正式提交** `0377445`。
- **意义**:从这一刻起,即使工作文件再被截断,`git show HEAD:core/data_provider.py` 就能秒级找回。

### 事件 8:推送到 GitHub——撞上"两条历史对不上"(约 23:0x)

- **现象**:`git push` 被拒绝。原因:你电脑上的 git 只有我刚建的 1 个提交,而 GitHub 上有你长期维护的 **74 个提交**(含自动测试、V10.0 发布等),**两者没有共同祖先**——是两条独立的线。
- **风险**:绝不能强推(会抹掉 GitHub 上 74 个提交 + 自动测试,不可逆)。
- **你的选择**:安全合并——保留 GitHub 上 74 个提交,把你电脑上的最新文件作为第 75 个提交叠加上去。
- **做法**:先打包备份你电脑这版 → 把本地接回 GitHub 的 74 个历史 → 把你的最新文件覆盖回去 → 提交 `d345c32` → 推送成功。**零历史丢失。**
- **插曲**:Git Bash 自带的解压工具在处理中文文件名时出错,改用 Python 解压解决。

### 事件 9:我说了缩写没解释,你火了(约 23:1x)

- 我甩了"LF/CRLF"两个缩写没翻译。你明确说再这样就不用我了。
- **补救**:把"**严禁裸甩技术缩写,必须紧跟大白话**"写进**用户级长期记忆 + 本项目约束**两份文件,列为最高优先级沟通红线。
- (这两个缩写的意思:代码每行结尾电脑会塞一个看不见的换行记号;你 Windows 电脑用两个字符,Git 存到网上时统一成一个,所以同一个文件网上显示的体积比你电脑上小一点,内容一模一样。)

### 事件 10:行情数据库 baostock.db 也不见了(约 23:38)→ 后续在回收站找到今天最新数据并救回(约 23:50)

- **现象**:你跑 hunter.py,报"数据库文件不存在: baostock.db"。你担心"今天刚同步的最新数据库还在不在本地"。
- **第一轮(顶替)**:从 antigravity 备份复制了一份(数据只到 7-21,旧 10 天),先让扫描器能跑。
- **第二轮(救回今天数据)——关键突破**:翻回收站,发现 **D 盘回收站里有完整的 baostock.db,573MB,数据到今天(2026-07-31),日线 275 万行,完整性检查通过**。C 盘回收站还有另一份(同样 7-31 数据)。你今天刚同步的最新数据**没丢,一直在回收站里**。
- **回收站元数据取证(读 $I 文件)**:D 盘那份的原始位置就是 `...\debugV7.1_for_workbuddy\data`(你的工作目录),**删除时间 = 2026-07-31 23:30:47**——正好是我做"安全合并推送"(`git reset --hard` + 解压覆盖)的时间点。
- **是不是 git reset 删的**:**不是,已证明**。`.gitignore` 排除了 `data/*.db`,我的提交和远程都没跟踪这个库 → `git reset --hard` 只动"被版本库跟踪"的文件,不碰这种不受跟踪的库;而且 git 删文件是直接删除(不进回收站)。回收站里出现它,说明走的是"送回收站"的路径。
- **谁送进回收站的**:WorkBuddy 有一层"安全删除"机制,会拦截删除操作、改送回收站(我后来删一个备份文件时亲眼看到它被拦截)。**这层机制反而救了你的数据**——如果直接永久删除,就真没了。确切是哪一步触发的还没定论(嫌疑是推送时那个失败的中文名解压步骤,或外部进程),但时间点跟我的推送操作重合,我不能完全排除自己的操作间接引发。**这条我会继续盯着查。**
- **已恢复**:D 盘回收站那份(7-31 今天数据)复制回 `data/baostock.db`,验证日线 275 万行、最新交易日 2026-07-31、完整性 ok。**无需重新同步。** 另备份一份到项目外 `_recovery_2026-07-31_data_provider/baostock.db.2026-07-31.recovered.bak`(防回收站被清空)。
- **额外线索**:被删的 data 文件夹里有个 `_empty_db_accident_20260726.db`(94KB,7-25)——提示 7/26 也发生过 db 事故,跟 data_provider 被截断同一天,可能有模式,值得留意。

---

## 二、系统健康度评估(有证据)

### 修复前(今晚事故的温床)——你的怀疑成立

| 维度 | 修复前状态 | 风险等级 |
|------|-----------|---------|
| 代码版本备份 | **从未提交过任何版本** | 🔴 极高(文件丢就找不回) |
| 行情数据库备份 | **没有任何备份**,且不进版本库 | 🔴 极高(全市场数据单点) |
| 截断源头 | 有外部程序偷偷删文件,**未查清** | 🟠 高(可能复发) |
| 防御机制 | 无自动守卫 | 🟠 高 |

**结论:修复前系统健康度确实严重不足。** 这就是为什么一个文件被清空,就引发了长达 3 小时的连环救火。

### 修复后(现在)——已显著改善

| 维度 | 修复后状态 | 证据 |
|------|-----------|------|
| 代码版本备份 | ✅ 已提交 + 推送 GitHub,体检无损坏 | 提交 `d345c32`,fsck 干净,上游已连 `origin/main` |
| 核心文件完整性 | ✅ 逐字节还原 + 在版本库中 | 8/8 函数匹配 pyc;HEAD 中 49273 字节 |
| 源码全量体检 | ✅ 无其他文件被截短 | core/ 下 18 个文件大小均正常 |
| 质量门禁 | ✅ 通过 | 92 文件 / 193 测试 / 0 红线 |
| 行情数据库 | ✅ 回收站救回今天(7-31)数据 + 项目外备份 | 573MB,日线275万行,最新交易日7-31,完整性ok;另有项目外备份 |
| 防截断守卫 | ✅ 已接入提交前检查 | `hooks/guard_source_shrink.py` |
| 沟通规范 | ✅ 已写入双份记忆 | 严禁裸甩缩写 |

### 还剩的坑(诚实清单)

1. **🟠 截断源头未查清**:data_provider.py 是被哪个外部程序截断的,始终没找到日志证据。**可能复发**。建议留意你的编辑器、网盘同步工具(OneDrive/百度网盘)在事发时段的活动。
2. **🟠 行情数据库仍需自动备份机制**:`baostock.db` 不进版本库(git 和 GitHub 都救不了它)。今晚能救回是因为:① 回收站里有(WorkBuddy 安全删除机制送的);② antigravity 有旧副本;③ 我手动备份了一份到项目外。但这都是"事后补救",不是"自动兜底"。建议:加一个定时把 `data/baostock.db` 镜像到备份目录的机制。另外,**data 文件夹在推送操作时间点(23:30:47)被整包删进回收站**,确切触发未定(疑失败的中文名解压步骤或外部进程),今后对 data/ 附近的操作要更谨慎。
3. **🟡 一个独立的旧测试失败**:`test_run_pipeline_no_ai`(GAP_H2 策略在测试环境出 0 命中)。已隔离确认与今晚事故无关,是更早就存在的问题,但属于潜在隐患。
4. **🟢 一些导出用 CSV 文件不在了**:旧的目录快照里提到过 7 个 CSV,现在没有。这些是可重新生成的导出产物,不影响扫描器运行,非阻塞。

---

## 三、给你的建议(按优先级)

1. **【高】给行情数据库加自动镜像**:今晚就证明了它是个单点。我可以帮你写一个简单的定时任务,每天把 `data/baostock.db` 复制一份到 antigravity 旁边的备份目录(只读副本,不动原备份)。要不要我做?
2. **【高】排查同步盘**:如果你项目目录在 OneDrive/百度网盘的同步范围内,强烈建议把项目目录**排除出同步**,或者至少把 `data/` 和 `core/` 排除——同步工具截断文件是今晚最可能的元凶。
3. **【中】保持提交习惯**:以后每次实质改动后,跟我说"commit"就行,我会提交(不会自动推 GitHub,除非你说"push")。这样任何文件再丢都能从版本库秒级找回。
4. **【低】那条旧测试失败**:有空了我可以帮你查根因,不急。

---

## 四、附:今晚的关键证据文件位置

- 恢复版核心文件:`core/data_provider.py`(50559 字节)
- 防截断守卫:`hooks/guard_source_shrink.py`
- 预防建议:`docs/accident_prevention_constraints.md`
- 清理弹窗调查报告:`docs/investigation_cleanup_popup_2026-07-31.md`
- 推送前备份(项目外):`D:\...\_prepush_backup_2026-07-31\local_HEAD.tar.gz`
- 恢复脚手架(项目外):`D:\...\_recovery_2026-07-31_data_provider\.recover_tmp\`
- GitHub 远程:`https://github.com/lhy438769415-wq/A`(提交 `d345c32`)
