# 数据安全第一铁律（DATA_SAFETY.md）

> 本文件是用户（A股量化交易员，非程序员）与 WorkBuddy（AI 助手）之间的**数据安全约定**。
> 任何会碰这个项目的 AI 助手，都必须先读本文件，并严格遵守下面四条铁律。

## 为什么要有这份文件（大白话背景）

你的行情数据库 `data/baostock.db`（约 550MB）是整个系统的命根子——一旦被改坏，多年积累的行情数据很难重建。

**2026-08-12 发生过一次"回收站惊魂"**：当天早上 08:16，你（或你前一天开着、今早自动恢复的 WorkBuddy 会话）让代理做"系统体检"。代理在执行任务时打开了 `baostock.db` 做结构检查，过程中产生了 SQLite 的辅助文件（`.db-wal` / `.db-shm`）和临时副本。这些文件被清理时，撞上了 WorkBuddy 平台那层"删除拦截 → 送回收站"的机制（叫"安全删除"，是平台的签名行为），于是被丢进了回收站。

**真相**：真身 `baostock.db` 始终完好（数据库完整性检查 = ok），只是辅助文件和临时副本进了回收站，看着吓人。但根因是——**代理在你没看管时跑了会碰库的任务，且操作对你不可见、不可控**。

下面四条铁律，就是为了防止这类事再发生。

---

## 铁律一：不碰生产库文件（复制 / 备份 / 移动 / 删除）

- 任何匹配 `data/*.db*`（含 `.db`、`.db-wal`、`.db-shm`）的文件，**除非你在这条消息里明确要求**，否则我**绝不**：
  - 复制（copy）
  - 备份（backup）
  - 移动（move）
  - 删除（delete / 送回收站）
- 这一条**特别覆盖平台的"修改前自动备份"行为**（日志里叫 `ModifyBackup` / `enableFileBackup`）：即使用户没要求备份，代理也**不主动触发**会复制整个库的动作。需要备份时，先问你、并在你指定的隔离位置做。

## 铁律二：碰 `data/` 之前先说，危险动作等你点头

- 任何会碰 `data/` 目录的操作，我先用**大白话**告诉你三件事：
  1. **我要干嘛**（例如"我要打开库做个结构体检"）
  2. **为什么要碰**
  3. **会不会产生临时文件 / 辅助文件**
- "写 / 备份 / 复制 / 删除"这类**改变状态**的动作，必须等你明确说"可以"才做。你不点头，我不动。

## 铁律三：碰完数据给大白话小结（每次）

每次碰过 `data/` 后，我用大白话给你一份小结，至少包含：
- 我做了什么；
- 你的**真身文件**（`baostock.db`）动没动；
- 有没有留下临时文件；
- 有没有东西进了回收站。

（这一条让我们的协作变成"你能看懂、能喊停"的模式，而不是黑箱。）

## 铁律四：减少触发（别在无人看管时跑数据重活）

- **不在无人看管时**跑会碰 `baostock.db` 的任务——包括：会话自动恢复后自作主张执行、后台定时任务、你离开电脑时。
- **数据同步 / 结构体检等重活，集中到你明确要求、且你在场知道的时间段**做。
- 如果会话恢复时，面临一个"会碰库"的待办，**先停下来问你**，不自动执行。

---

## 根因小知识（看懂回收站里那些文件）

- **`.db-wal` / `.db-shm` 是什么？** 这是 SQLite（装你库的软件）在 WAL 模式下自动生成的"便签"文件。即使只是"只读看一眼"库，也会产生它们。平台把它们的"出现又消失"当成"有文件被删"，于是送回收站。它们不是账本本身。
- **Temp 里那两个 563MB 副本是什么？** 是平台"改前自动备份"生成的**完整库副本**，清理时送回收站。和你的真身库是两码事。
- **真身坏没坏？** 没坏。历次事件后数据库完整性检查都是 `ok`。

## 这份铁律的边界（诚实说明）

- 它能管住**我（WorkBuddy）主动发起的操作**——不复制、不备份、先问你、给小结、不无人看管乱跑。
- 但平台"改前自动备份"是**平台默认行为**，目前没有"别备份 / 别拦截 `data/` 目录"的文件级开关（已查过 `~/.workbuddy/settings.json`，无此配置）。这条需要向平台提需求才能根治。
- 所以：**铁律 + 你的警惕（留意回收站）** 是双保险。发现异常，随时让我查。
